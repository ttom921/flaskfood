export enum MessageBoxButton {
  Ok = 0,
  OkCancel = 1,
  YesNo = 2,
  AcceptReject = 3
}
export enum MessageBoxStyle {
  Simple = 0,
  Full = 1,
  Loading = 2,
};

export enum RespCode {
  UNDEFINERROR = 999 //# 未定義的錯誤
}
